page
===
select @pageTag(){
#use("cols")#
@}
	from ${tableName} where #use("condition")#
cols
===
    <templete id="1" dataSource="columnDatas" type="for" trim=",">
        ${column_name},
    </templete>
condition
===
	1 = 1
    <templete id="1" dataSource="columnDatas" type="for" trim=",">
        @if(!isEmpty(${propertyName})){
             and ${column_name}=#${propertyName}#
        @}
    </templete>
